# 快速开始指南

本文档帮助您快速搭建并运行房源图片信息汇总工具。

---

## 一、环境准备

### 必需工具
- **Node.js**：v18.0.0 或更高版本
- **npm** 或 **pnpm**（推荐pnpm，更快）
- **代码编辑器**：VS Code（推荐）

### 检查安装
```bash
node -v    # 应该显示 v18.0.0 或更高
npm -v     # 应该显示 9.0.0 或更高
```

---

## 二、创建项目

### 1. 初始化Next.js项目
```bash
npx create-next-app@latest fy --typescript --tailwind --app --no-src-dir
cd fy
```

创建时的选项：
- ✅ TypeScript
- ✅ Tailwind CSS
- ✅ App Router
- ❌ src/ directory
- ✅ import alias (@/*)

### 2. 安装依赖
```bash
npm install antd @ant-design/icons xlsx axios openai
```

或使用pnpm：
```bash
pnpm add antd @ant-design/icons xlsx axios openai
```

**依赖说明**：
- `antd`: UI组件库（表格、按钮、上传等）
- `@ant-design/icons`: Ant Design图标
- `xlsx`: Excel文件生成（前端）
- `axios`: HTTP请求库（调用OCR服务）
- `openai`: OpenAI SDK（用于调用魔塔API）

---

## 三、配置环境变量

### 创建 .env.local 文件
在项目根目录创建 `.env.local` 文件：

```bash
# 百度OCR配置（必需）
BAIDU_API_KEY=你的百度API_KEY
BAIDU_SECRET_KEY=你的百度SECRET_KEY

# 魔塔（ModelScope）AI配置（必需）
MODELSCOPE_ACCESS_TOKEN=ms-571153f6-c26d-4dba-b4c2-ec6a88450519
```

### 获取API密钥

#### 1. 百度OCR API
1. 访问：https://cloud.baidu.com/
2. 注册/登录账号
3. 进入「控制台」→「产品服务」→「文字识别OCR」
4. 创建应用，获取 API Key 和 Secret Key
5. **免费额额度**：1000次/天（足够测试使用）

**步骤详细**：
- 选择「通用文字识别（高精度版）」
- 应用名称：房源识别工具
- 接口选择：通用文字识别（高精度）
- 文字识别→创建应用→获取密钥

#### 2. 魔塔（ModelScope）API
1. 访问：https://modelscope.cn/
2. 注册/登录账号（支持手机号/GitHub）
3. 进入「个人中心」→「访问令牌」
4. 点击「创建新令牌」
5. 复制Token（格式：ms-xxxxxxxxx）

**魔塔API优势**：
- ✅ OpenAI兼容接口，代码简单
- ✅ 中文理解能力强
- ✅ 国内访问，无需代理
- ✅ 新用户有免费额度
- ✅ 多种模型可选（qwen-turbo推荐）

**推荐模型**：
- `qwen-turbo`: 速度快，成本低（推荐）
- `qwen-plus`: 平衡性能
- `qwen-max`: 最高精度

---

## 四、创建项目结构

### 1. 创建目录结构
```bash
mkdir -p lib components types app/api/recognize
```

### 2. 创建TypeScript类型文件

**types/property.ts**
```typescript
export interface Property {
  id: string;
  imageId?: string;
  region: string;
  building: string;
  roomNumber: string;
  area: string;
  price: string;
  propertyFee: string;
  commission: string;
  remarks: string;
  confidence: number;
  createdAt: string;
}

export interface ImageFile {
  id: string;
  name: string;
  base64: string;
  file: File;
}

export interface RawProperty {
  region: string;
  building: string;
  roomNumber: string;
  area: string;
  price: string;
  propertyFee: string;
  commission: string;
  remarks: string;
}
```

### 3. 创建OCR服务

**lib/ocr.ts**
```typescript
import axios from 'axios';

export async function recognizeImage(base64Image: string): Promise<string> {
  const accessToken = await getBaiduAccessToken();
  
  // 去除base64前缀
  const imageData = base64Image.includes(',') 
    ? base64Image.split(',')[1] 
    : base64Image;
  
  const response = await axios.post(
    `https://aip.baidubce.com/rest/2.0/ocr/v1/accurate_basic?access_token=${accessToken}`,
    `image=${encodeURIComponent(imageData)}`,
    {
      headers: {
        'Content-Type': 'application/x-www-form-urlencoded'
      }
    }
  );
  
  if (response.data.error_code) {
    throw new Error(response.data.error_msg || 'OCR识别失败');
  }
  
  const words = response.data.words_result || [];
  return words.map((item: any) => item.words).join('\n');
}

async function getBaiduAccessToken(): Promise<string> {
  const response = await axios.post(
    'https://aip.baidubce.com/oauth/2.0/token',
    null,
    {
      params: {
        grant_type: 'client_credentials',
        client_id: process.env.BAIDU_API_KEY,
        client_secret: process.env.BAIDU_SECRET_KEY
      }
    }
  );
  
  return response.data.access_token;
}
```

### 4. 创建AI解析服务

**lib/ai-parser.ts**
```typescript
import OpenAI from 'openai';
import { RawProperty } from '@/types/property';

// 使用魔塔（ModelScope）API - OpenAI兼容接口
const client = new OpenAI({
  apiKey: process.env.MODELSCOPE_ACCESS_TOKEN || 'ms-571153f6-c26d-4dba-b4c2-ec6a88450519',
  baseURL: 'https://api-inference.modelscope.cn/v1/'
});

export async function parseProperty(ocrText: string): Promise<RawProperty> {
  const prompt = `你是房源信息提取助手。请从以下OCR识别的文本中，精准提取8个字段：

1. 区域：房源所属行政区域（瑶海区、经开区、蜀山区、庐阳区、包河区、新站区）
2. 楼盘：楼盘名称全称
3. 房号：房间编号（保留原格式）
4. 面积：面积+单位（如252㎡）
5. 价格：租金价格+单位（如42元/㎡/天）
6. 物业费：物业费+单位（如3.5元/㎡/月）
7. 佣金：佣金政策
8. 备注：关键补充信息

OCR识别结果：
${ocrText}

请以JSON格式返回（仅返回JSON，不要其他内容）：
{
  "region": "",
  "building": "",
  "roomNumber": "",
  "area": "",
  "price": "",
  "propertyFee": "",
  "commission": "",
  "remarks": ""
}`;

  const completion = await client.chat.completions.create({
    model: 'qwen-turbo', // 推荐：速度快，成本低
    // 可选其他模型：qwen-plus（平衡）、qwen-max（最高精度）
    messages: [
      { role: 'system', content: '你是一个专业的房源信息提取助手，擅长从文本中提取结构化数据。' },
      { role: 'user', content: prompt }
    ],
    temperature: 0.1, // 降低随机性，提高准确性
    max_tokens: 1024
  });
  
  const responseText = completion.choices[0]?.message?.content || '';
  
  // 提取JSON（处理可能的markdown代码块）
  const jsonMatch = responseText.match(/\{[\s\S]*\}/);
  if (!jsonMatch) {
    throw new Error('AI解析失败：无法提取JSON');
  }
  
  return JSON.parse(jsonMatch[0]);
}
```

**魔塔API说明**：
- 完全兼容OpenAI接口规范
- 只需修改 `apiKey` 和 `baseURL`
- 支持流式输出（如需要）
- 中文理解能力优秀

### 5. 创建数据规整服务

**lib/data-normalizer.ts**
```typescript
import { Property, RawProperty } from '@/types/property';

const VALID_REGIONS = ['瑶海区', '经开区', '蜀山区', '庐阳区', '包河区', '新站区'];

export function normalizeProperty(raw: RawProperty): Property {
  return {
    id: generateId(),
    region: normalizeRegion(raw.region),
    building: raw.building?.trim() || '识别失败',
    roomNumber: raw.roomNumber?.trim() || '识别失败',
    area: normalizeArea(raw.area),
    price: normalizePrice(raw.price),
    propertyFee: normalizePropertyFee(raw.propertyFee),
    commission: raw.commission?.trim() || '无',
    remarks: raw.remarks?.trim() || '无',
    confidence: calculateConfidence(raw),
    createdAt: new Date().toISOString()
  };
}

function normalizeRegion(region: string): string {
  if (!region) return '识别失败';
  
  const regionMap: Record<string, string> = {
    '摇海区': '瑶海区',
    '瑶海': '瑶海区',
    '经济开发区': '经开区',
    '经开': '经开区',
    '蜀山': '蜀山区',
    '庐阳': '庐阳区',
    '包河': '包河区',
    '新站': '新站区'
  };
  
  const normalized = regionMap[region] || region;
  return VALID_REGIONS.includes(normalized) ? normalized : region;
}

function normalizeArea(area: string): string {
  if (!area) return '无';
  if (area.includes('㎡') || area.includes('工位')) return area;
  if (/^\d+$/.test(area)) return area + '㎡';
  return area;
}

function normalizePrice(price: string): string {
  if (!price) return '无';
  if (price.includes('面议') || price.includes('面谈')) return '面议';
  return price;
}

function normalizePropertyFee(fee: string): string {
  if (!fee) return '无';
  if (fee.includes('含') || fee.includes('包含')) return '含在租金内';
  if (fee.includes('免')) return '免物业费';
  return fee;
}

function calculateConfidence(raw: RawProperty): number {
  let score = 0;
  if (raw.region && raw.region !== '识别失败') score += 0.15;
  if (raw.building && raw.building !== '识别失败') score += 0.15;
  if (raw.roomNumber && raw.roomNumber !== '识别失败') score += 0.15;
  if (raw.price && raw.price !== '无') score += 0.15;
  if (raw.area && raw.area !== '无') score += 0.1;
  if (raw.propertyFee && raw.propertyFee !== '无') score += 0.1;
  if (raw.commission && raw.commission !== '无') score += 0.1;
  if (raw.remarks && raw.remarks !== '无') score += 0.1;
  return score;
}

function generateId(): string {
  return `prop-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
}
```

### 6. 创建Excel导出服务

**lib/excel-export.ts**
```typescript
import * as XLSX from 'xlsx';
import { Property } from '@/types/property';

export function exportToExcel(properties: Property[], fileName?: string) {
  const data = properties.map(p => ({
    '区域': p.region,
    '楼盘': p.building,
    '房号': p.roomNumber,
    '面积(㎡)': p.area,
    '价格': p.price,
    '物业费(元/㎡/月)': p.propertyFee,
    '佣金': p.commission,
    '备注': p.remarks
  }));
  
  const worksheet = XLSX.utils.json_to_sheet(data);
  
  worksheet['!cols'] = [
    { wch: 12 }, { wch: 20 }, { wch: 12 }, { wch: 12 },
    { wch: 18 }, { wch: 18 }, { wch: 18 }, { wch: 30 }
  ];
  
  const workbook = XLSX.utils.book_new();
  XLSX.utils.book_append_sheet(workbook, worksheet, '房源汇总');
  
  const date = new Date().toISOString().split('T')[0].replace(/-/g, '');
  const finalFileName = fileName || `房源汇总表_${date}.xlsx`;
  
  XLSX.writeFile(workbook, finalFileName);
}
```

### 7. 创建API路由

**app/api/recognize/route.ts**
```typescript
import { NextRequest, NextResponse } from 'next/server';
import { recognizeImage } from '@/lib/ocr';
import { parseProperty } from '@/lib/ai-parser';
import { normalizeProperty } from '@/lib/data-normalizer';

export async function POST(request: NextRequest) {
  try {
    const { image } = await request.json();
    
    if (!image) {
      return NextResponse.json({
        success: false,
        error: '缺少图片数据'
      }, { status: 400 });
    }
    
    // OCR识别
    const ocrText = await recognizeImage(image);
    
    // AI解析
    const rawProperty = await parseProperty(ocrText);
    
    // 数据规整
    const property = normalizeProperty(rawProperty);
    
    return NextResponse.json({
      success: true,
      data: property
    });
    
  } catch (error: any) {
    console.error('识别失败:', error);
    return NextResponse.json({
      success: false,
      error: error.message || '识别失败'
    }, { status: 500 });
  }
}
```

---

## 五、启动项目

### 1. 开发环境运行
```bash
npm run dev
```

访问：http://localhost:3000

### 2. 构建生产版本
```bash
npm run build
npm run start
```

---

## 六、测试API

### 使用curl测试识别接口
```bash
curl -X POST http://localhost:3000/api/recognize \
  -H "Content-Type: application/json" \
  -d '{"image":"data:image/jpeg;base64,/9j/4AAQSkZJRg..."}'
```

### 预期响应
```json
{
  "success": true,
  "data": {
    "id": "prop-1234567890-abc123",
    "region": "瑶海区",
    "building": "万事通大厦",
    "roomNumber": "1005",
    "area": "252㎡",
    "price": "42元/㎡/天",
    "propertyFee": "3.5元/㎡/月",
    "commission": "全佣金",
    "remarks": "含家具，电梯口",
    "confidence": 0.96,
    "createdAt": "2026-02-15T10:00:00.000Z"
  }
}
```

---

## 七、下一步开发

现在基础架构已搭建完成，接下来需要开发前端组件：

### 待开发组件列表
1. **UploadArea.tsx** - 图片上传组件
2. **ImagePreview.tsx** - 图片预览组件
3. **PropertyTable.tsx** - 房源表格组件
4. **EditModal.tsx** - 编辑弹窗组件
5. **ExportButton.tsx** - 导出按钮组件
6. **app/page.tsx** - 主页面（整合所有组件）

参考《技术实现方案-NextJS.md》中的组件代码实现。

---

## 八、常见问题排查

### 问题1：API调用失败
**检查**：
- [ ] .env.local文件是否存在
- [ ] 环境变量是否正确配置
- [ ] API密钥是否有效
- [ ] 网络连接是否正常

### 问题2：OCR识别失败
**可能原因**：
- 百度API配额用尽（免费版1000次/天）
- 图片格式不支持
- 图片太大（>4MB建议压缩）

### 问题3：AI解析失败
**可能原因**：
- 魔塔API Token无效或过期
- API调用额度用尽
- OCR文本质量太差（图片模糊）
- 网络连接问题

**解决方法**：
- 检查Token是否正确
- 登录魔塔官网查看额度
- 尝试更换模型（qwen-plus或qwen-max）
- 优化Prompt提示词

### 问题4：端口被占用
```bash
# 查看端口占用
lsof -i :3000

# 杀死进程
kill -9 <PID>

# 或使用其他端口
npm run dev -- -p 3001
```

---

## 九、推荐VS Code插件

- **ES7+ React/Redux/React-Native snippets** - React代码片段
- **TypeScript Error Translator** - TypeScript错误翻译
- **Tailwind CSS IntelliSense** - Tailwind自动补全
- **Prettier** - 代码格式化
- **ESLint** - 代码检查

---

## 十、开发资源

### 官方文档
- Next.js: https://nextjs.org/docs
- Ant Design: https://ant.design/components/overview-cn/
- TypeScript: https://www.typescriptlang.org/docs/

### API文档
- 百度OCR: https://cloud.baidu.com/doc/OCR/index.html
- 魔塔API: https://modelscope.cn/docs/
- OpenAI API参考: https://platform.openai.com/docs/api-reference

---

## 十一、获取帮助

### 遇到问题时
1. 查看控制台错误信息
2. 检查Network标签查看API请求
3. 查看.env.local配置
4. 参考本项目的其他文档

### 技术支持
- Next.js Discord: https://nextjs.org/discord
- GitHub Issues: 项目仓库
- Stack Overflow: 标签 [next.js] [typescript]

---

**恭喜！** 您已经完成了基础环境的搭建，可以开始开发前端界面了。
